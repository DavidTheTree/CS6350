This contains the all code used for HW2, normally, each file works for different problems/datasets, there are no other special instructions to run this.
make sure the data directory is right and the model is installed, then the codes are good to be processed.
